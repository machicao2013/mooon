include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -L../util -lutil
AM_LDFLAGS  += -fPIC -shared -pthread -lrt -ldl
AM_CXXFLAGS += -fPIC

moduledir=$(prefix)/lib
module_PROGRAMS = libsys.so

libsys_so_SOURCES =

install-data-hook:
	cp -r $(top_srcdir)/../include/sys $(prefix)/include
