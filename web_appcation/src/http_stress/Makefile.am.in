include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -lutil -lsys -lnet -ldispatcher -lhttp_parser
AM_LDFLAGS  += -fPIC -shared
AM_CXXFLAGS += -fPIC

bindir=$(prefix)/bin
bin_PROGRAMS = http_stress

http_stress_SOURCES =
