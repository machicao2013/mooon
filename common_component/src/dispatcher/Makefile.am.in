include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -lutil -lsys -lnet -lplugin_log4cxx -lplugin_tinyxml
AM_LDFLAGS  += -fPIC -shared
AM_CXXFLAGS += -fPIC

moduledir=$(prefix)/lib
module_PROGRAMS = libdispatcher.so

libdispatcher_so_SOURCES =

install-data-hook:
	mkdir -p $(prefix)/include&&cp -r $(top_srcdir)/../include/dispatcher $(prefix)/include/

uninstall-hook:
	rm -fr $(prefix)/include/dispatcher