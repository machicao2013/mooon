include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += ../../server/libserver.a $(MOOON_HOME)/lib/libnet.a $(MOOON_HOME)/lib/libsys.a $(MOOON_HOME)/lib/libutil.a
#LDADD      += /usr/lib/x86_64-linux-gnu/librt.a
LDADD      += -lrt

AM_LDFLAGS  += 
AM_CXXFLAGS += 

bindir = $(prefix)/bin
bin_PROGRAMS = center
center_SOURCES =
