include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -L../util -lutil -L../sys -lsys -L../net -lnet -L../server -lserver -L../plugin/plugin_log4cxx -lplugin_log4cxx -L../plugin/plugin_tinyxml -lplugin_tinyxml
AM_LDFLAGS  += -ldl -L../scheduler -lscheduler -L../receiver -lreceiver -L../sender -lsender
AM_CXXFLAGS +=

bindir=$(prefix)/bin
bin_PROGRAMS = container

container_SOURCES = 
