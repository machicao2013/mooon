include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -lutil -lsys -lnet -lhttp_parser -ldispatcher
AM_LDFLAGS  += -fPIC
AM_CXXFLAGS += -fPIC

bindir=$(prefix)/bin
bin_PROGRAMS = http_stress

http_stress_SOURCES =
