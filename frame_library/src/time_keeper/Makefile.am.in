include $(top_srcdir)/Make.rules
AUTOMAKE_OPTIONS= foreign

INCLUDES   +=
LDADD      += -L../util -lutil -L../sys -lsys
AM_LDFLAGS  += -fPIC -shared -pthread -lrt
AM_CXXFLAGS += -fPIC

moduledir=$(prefix)/lib
module_PROGRAMS = libtime_keeper.so

libtime_keeper_so_SOURCES = 
